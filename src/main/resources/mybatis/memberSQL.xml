<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<!-- ID 중복 확인 -->
	<select id="confirmId" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				ID
			FROM
				MEMBER
			WHERE
				ID = #{ID}
		]]>
	</select>
	
	<!-- 회원등록 -->
	<insert id="insertMember" parameterType="hashmap">
		<![CDATA[
			INSERT INTO MEMBER
			(
				ID,
				PASSWD,
				PHONE,
				BIRTH,
				EMAIL,
				POSTCODE,
				ADDRESS1,
				ADDRESS2,
				RESERVE,
				JOINDATE,
				DELFLAG
			)
			VALUES
			(
				#{ID},
				#{PASSWD},
				#{PHONE},
				#{BIRTH},
				#{EMAIL},
				#{POSTCODE},
				#{ADDRESS1},
				#{ADDRESS2},
				0,
				SYSDATE,
				'N'
			)
		]]>
	</insert>
	
	<!-- ID 찾기 -->
	<select id="searchId" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				ID
			FROM
				MEMBER
			WHERE
				NAME = #{NAME} AND
				PHONE = #{PHONE} AND
				DELFLAG = 'N'
		]]>
	</select>
	
	<!-- PW 찾기 -->
	<select id="searchId" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				PASSWD
			FROM
				MEMBER
			WHERE
				ID = #{ID} AND
				NAME = #{NAME} AND
				PHONE = #{PHONE} AND
				DELFLAG = 'N'
		]]>
	</select>
	
	<!-- 회원 찾기 -->
	<select id="searchId" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				ID,
				PASSWD,
				NAME,
				PHONE,
				BIRTH,
				EMAIL,
				POSTCODE,
				ADDRESS1,
				ADDRESS2,
				RESERVE,
				JOINDATE
			FROM
				MEMBER
			WHERE
				ID = #{ID} AND
				DELFLAG = 'N'
		]]>
	</select>
	
	<!-- 회원 정보 변경 (id, 이름, 생년월일, 적립금, 가입날짜는 변경불가) -->
	<update id="updateMember" parameterType="hashmap">
		<![CDATA[
			UPDATE MEMBER 
			SET
				PASSWD = #{PASSWD},
				PHONE = #{PHONE},
				EMAIL = #{EMAIL},
				POSTCODE = #{POSTCODE},
				ADDRESS1 = #{ADDRESS1},
				ADDRESS2 = #{ADDRESS2}
			WHERE
				ID = #{ID}	
		]]>
	</update>
	
	<!-- PW 확인 -->
	<select id="checkPw" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT
				PASSWD
			FROM
				MEMBER
			WHERE
				ID = #{ID}
		]]>
	</select>
	
	<!-- 회원 삭제 -->
	<update id="dropMember" parameterType="hashmap">
		<![CDATA[
			UPDATE MEMBER 
			SET
				DELFLAG = 'Y'
			WHERE
				ID = #{ID}	
		]]>
	</update>
	
</mapper>